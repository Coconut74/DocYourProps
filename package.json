{
  "name": "docplugin",
  "version": "0.1.0",
  "description": "Figma plugin DocPlugin",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json --watch"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.100.0",
    "typescript": "^5.4.5"
  }
}
